import { readFileSync } from "node:fs";
import { describe, expect, it } from "vitest";
const source = readFileSync(new URL("../client/src/pages/Assessment.tsx", import.meta.url), "utf8");
describe("assessment enhancements", () => { it("provides a seven-step Robert tips panel", () => { expect(source).toContain("const assessmentTips = ["); expect(source).toContain("Robert’s tips for this step"); expect(source).toContain('aria-controls="robert-step-tips"'); expect(source).toContain("assessmentTips[step].bullets.map"); expect(source).toContain('role="progressbar"'); expect(source).toContain("assessment-completion-meter"); expect(source).toContain("remainingSteps"); }); it("marks optional business fields and provides examples", () => { expect(source).toContain('optional placeholder="e.g., Jordan Lee"'); expect(source).toContain('optional placeholder="e.g., (555) 123-4567"'); expect(source).toContain('placeholder="e.g., name@company.com"'); expect(source).toContain('placeholder="e.g., 250,000"'); expect(source).toContain('placeholder="e.g., 1,200,000"'); expect(source).toContain('placeholder="e.g., 45,000"'); }); it("saves and restores progress only in same-device local storage", () => { expect(source).toContain('const ASSESSMENT_DRAFT_KEY = "dreams-score-assessment-draft-v1"'); expect(source).toContain("window.localStorage.setItem(ASSESSMENT_DRAFT_KEY"); expect(source).toContain("readSavedAssessmentDraft"); expect(source).toContain("Saved progress stays only in this browser on this device."); expect(source).toContain("Start fresh"); }); });
